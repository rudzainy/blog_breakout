<p id="notice"><%= notice %></p>

<p>
  <strong>Post:</strong>
  <%= @post.post %>
</p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>User:</strong>
  <%= @post.user_id %>
</p>
<div id="comments">
	<% @comments.each do |comment| %>
		<p><%= comment.body %></p>
	<% end %>
</div>
<div id="newComment"></div>


<%= link_to("Add Comment", new_comment_path(@post), remote: true ) %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
